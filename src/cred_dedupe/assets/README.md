This directory is used to store application assets.

Place your icon file here as:

    creddedupe.ico

The application will attempt to load this file at runtime, and the packaging
configuration includes `assets/*.ico` so it can be bundled with future
distributions or binaries.
